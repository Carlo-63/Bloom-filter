#include <stdint.h>
#include <stdio.h>

#include "hash.h"
#include "bloom_filter.h"

#define NUM_BITS 1024 
#define NUM_HASH 3 // Numero di funzioni di Hashing

int main() {
    uint8_t bloom[NUM_BITS / 8] = {0};

    // Il testing può essere effettuato qui

    return 0;
}
